!function(a){"use strict";window.App={},a(window).on("load",function(t){a(".preloader").fadeOut("slow")}),a(document).on("click","a.page-scroll",function(t){var l=a(this);a("html, body").stop().animate({scrollTop:a(l.attr("href")).offset().top-0},1250,"easeInOutExpo"),t.preventDefault()}),a(".nav ul li a").on("click",function(){a(".hamburger-toggle:visible").click()}),a("#mainMenu").affix({offset:{top:50}}),a("body").scrollspy({target:".nav-container",offset:50});var t=document.querySelectorAll(".parallax .bg-image-placer"),l=.5;window.onscroll=function(){[].slice.call(t).forEach(function(a,t){var e=window.pageYOffset,o="translate3d(0px,"+e*l+"px, 0px)";a.style.transform=o})},a(".bg-image-placer").each(function(){var t=a(this).children("img").attr("src");a(this).css("background",'url("'+t+'")').css("background-position","initial").css("opacity","1")}),a(".owl-carousel").owlCarousel({loop:!0,margin:10,nav:!1,responsive:{0:{items:1},600:{items:1},1e3:{items:1}}}),a("[data-toggle-class]").each(function(){var t=a(this),l=t.attr("data-toggle-class").split("|");a(l).each(function(){var l=t,e=[],e=this.split(";"),o="",s="";2===e.length&&(s=e[0],o=e[1],a(l).on("click",function(){return l.hasClass("toggled-class")?l.removeClass("toggled-class"):l.toggleClass("toggled-class"),a(s).toggleClass(o),!1}))})}),App.parallaxs=function(){return{obj:null}}(),void 0!=document.getElementById("main_slide_3")&&(App.parallaxs.obj=new Parallax(document.getElementById("main_slide_3")))}(jQuery);

// new js
$(document).ready(function() {
	// equalheight function.
	equalheight = function(container){

	var currentTallest = 0,
	     currentRowStart = 0,
	     rowDivs = new Array(),
	     $el,
	     topPosition = 0;
	 $(container).each(function() {

	   $el = $(this);
	   $($el).height('auto')
	   topPostion = $el.position().top;

	   if (currentRowStart != topPostion) {
	     for (currentDiv = 0 ; currentDiv < rowDivs.length ; currentDiv++) {
	       rowDivs[currentDiv].height(currentTallest);
	     }
	     rowDivs.length = 0; // empty the array
	     currentRowStart = topPostion;
	     currentTallest = $el.height();
	     rowDivs.push($el);
	   } else {
	     rowDivs.push($el);
	     currentTallest = (currentTallest < $el.height()) ? ($el.height()) : (currentTallest);
	  }
	   for (currentDiv = 0 ; currentDiv < rowDivs.length ; currentDiv++) {
	     rowDivs[currentDiv].height(currentTallest);
	   }
	 });
	}

	$(window).on('load', function() {
	  equalheight('.equal');
	});


	$(window).on('load', function(){
	  equalheight('.equal');
	});

	// portfolio
	$('.filters ul li').click(function(){
	  $('.filters ul li').removeClass('active');
	  $(this).addClass('active');
	  
	  var data = $(this).attr('data-filter');
	  $grid.isotope({
	    filter: data
	  })
	});

	var $grid = $(".grid").isotope({
	  itemSelector: ".all",
	  percentPosition: true,
	  masonry: {
	    columnWidth: ".all"
	  }
	});

	$('.map-section').click(function() {
		$('.map-canvas').slideToggle();
		$('.google-map').toggleClass('js-active');
	});

	$('.hamburger-toggle').click(function(){
		$('body').toggleClass('scrollOFF');
		$('.main-container').toggleClass('filter');
	});

	//windows scroll function and get windows size
	$(window).scroll(function(){
		var windowsize = $(window).width();
    	if (windowsize >= 800) {
		    if ($(window).scrollTop() >= 1) {
		    	// $('body').toggleClass('');
		    	$('.nav-container .nav .logo').css({
		    		'max-height':'60px'
		    	});
		    	$('.menu-horizontal').css({
		    		'margin':'9px 0px'
		    	});
		    } else {
		    	$('.nav-container .nav .logo').css({
		    		'max-height':'90px'
		    	});
		    	$('.menu-horizontal').css({
		    		'margin':'24px 0px'
		    	});
		    }
		}
	});
});